<h1>Previous Answers Already Given</h1>

<p><%= link_to 'Add a New Answer', new_answer_url %></p>

<table>
  <thead>
    <tr>
      <th>Person</th>
      <th>Question</th>
      <th>Answer</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @answers.each do |answer| %>
    <tr>
      <td><%= @person.name %></td>
      <td><%= answer.question.q %></td>
      <td><%= answer.a %></td>
    </tr>
  <% end %>
  </tbody>
</table>

